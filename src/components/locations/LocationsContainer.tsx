import { Container } from "@chakra-ui/react"

export const LocationsContainer = () => {
  return (
    <Container
      borderWidth="1px"
      borderColor="border.disabled"
      color="fg.disabled"
    >
      Locations
    </Container>
  )
}
